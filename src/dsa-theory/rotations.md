# Rotations

Rotations are used to rebalance the tree while preserving the binary-search-tree ordering.

- Left Rotation
- Right Rotation

See `src/core/RBTree.ts` for the implementations of `rotateLeft` and `rotateRight`.
